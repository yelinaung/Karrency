<resources>

  <!-- Base application theme. -->
  <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
    <!-- Customize your theme here. -->
    <!--<item name="actionOverflowButtonStyle">@style/ActionBarOverFlow</item>-->
    <item name="android:actionOverflowButtonStyle">@style/ActionBarOverFlow</item>

    <item name="android:actionBarTabTextStyle">@style/AppTheme.TabsText</item>

    <!--
    <item name="android:spinnerDropDownItemStyle">@style/ActionBarSpinnerStyle</item>
    <item name="actionDropDownStyle">@style/ActionBarSpinnerStyle</item>
    -->
  </style>

  <!-- ActionBar Overflow -->
  <style name="ActionBarOverFlow" parent="android:Widget.ActionButton.Overflow">
    <item name="android:src">@drawable/ic_action_overflow</item>
  </style>

  <!-- ActionBar Spinner Style -->
  <!--
  <style name="ActionBarSpinnerStyle" parent="Widget.AppCompat.Spinner.DropDown.ActionBar">
      <item name="android:textAppearance">@style/ActionBarSpinnerTextStyle</item>
  </style>
  -->

  <!-- ActionBar Spinner Text Style -->
  <!--
  <style name="ActionBarSpinnerTextStyle" parent="@android:style/Widget">
      <item name="android:textColor">@android:color/white</item>
  </style>
  -->

  <!-- ActionBar Text Style -->
  <style name="AppTheme.TabsText" parent="android:Widget.ActionBar.TabText">
    <item name="android:textSize">20sp</item>
    <item name="android:textColor">@android:color/black</item>
  </style>
</resources>
